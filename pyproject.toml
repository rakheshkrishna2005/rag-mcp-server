[project]
name = "copilot-rag-mcp"
version = "1.0.0"
description = "Python MCP server for RAG with FAISS"
requires-python = ">=3.12"
dependencies = [
  "mcp",
  "faiss-cpu",
  "sentence-transformers",
  "numpy",
  "pypdf",
  "beautifulsoup4",
]
